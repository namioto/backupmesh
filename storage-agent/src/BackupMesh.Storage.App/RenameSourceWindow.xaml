<Window x:Class="BackupMesh.Storage.App.RenameSourceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BackupMesh.Storage.App"
        Title="{local:Loc Text_RenameSourceAgent_7BF263}" Width="420" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        AutomationProperties.AutomationId="RenameSourceWindow">
    <StackPanel Margin="20">
        <TextBlock Text="{local:Loc Text_Displayname_2B7F6A}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBlock Text="{local:Loc Text_Thisonlychangeswhatthetrayshow_F822F2}" TextWrapping="Wrap" Foreground="#607086" Margin="0,0,0,10"/>
        <TextBox x:Name="DisplayNameText" Margin="0,0,0,16" AutomationProperties.AutomationId="RenameSourceTextBox"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{local:Loc Text_Cancel_19766E}" Click="OnCancelClick" IsCancel="True" Margin="0,0,10,0" Padding="10,4" AutomationProperties.AutomationId="RenameSourceCancelButton"/>
            <Button Content="{local:Loc Text_Save_1509F5}" Click="OnSaveClick" Style="{StaticResource PrimaryButtonStyle}" IsDefault="True" Padding="10,4" AutomationProperties.AutomationId="RenameSourceSaveButton"/>
        </StackPanel>
    </StackPanel>
</Window>
